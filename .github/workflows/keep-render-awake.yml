name: Keep DS2API Awake
on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping render
        run: |
          curl -L https://ds2api-huww.onrender.com
